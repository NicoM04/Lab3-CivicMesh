name: Agent - Bug Reviewer

# Ejecutable manualmente desde GitHub Actions (workflow_dispatch) y una vez
# al día (schedule). Nunca corrige código ni decide cambios de protocolo:
# solo reporta hallazgos verificables como issues (ver scripts/agents/README.md).
on:
  workflow_dispatch:
  schedule:
    - cron: "30 13 * * *" # una vez al día, desfasado del Documentador

permissions:
  contents: read
  issues: write
  models: read # permite usar GitHub Models con el GITHUB_TOKEN automático

jobs:
  run-bug-reviewer:
    name: run-bug-reviewer
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Ejecutar agente Revisor de Bugs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Ver agent-documenter.yml: mismo esquema de proveedor de IA
          # (GitHub Models por defecto, overridable con secrets propios).
          AGENT_API_URL: ${{ secrets.AGENT_API_URL || 'https://models.github.ai/inference/chat/completions' }}
          AGENT_API_KEY: ${{ secrets.AGENT_API_KEY || secrets.GITHUB_TOKEN }}
          AGENT_MODEL: ${{ secrets.AGENT_MODEL || 'openai/gpt-4o-mini' }}
        run: python -m scripts.agents.bug_reviewer
