# 2/2 — On every code change: run tests inside the published `:base` image from
# GHCR (same ref as build-base-container.yml).
#
# Job graph: ensure-base-image → unit-tests
#                              → integration-tests
name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: [main, master]

permissions:
  contents: read
  packages: read

jobs:
  ensure-base-image:
    name: Verify GHCR base image exists
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.resolve.outputs.image }}
    steps:
      - id: resolve
        name: Resolve GHCR image reference
        run: |
          repo_lc=$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')
          img="ghcr.io/${repo_lc}:base"
          echo "image=${img}" >> "$GITHUB_OUTPUT"
          {
            echo "### CI base image"
            echo "Expected image: \`${img}\`"
            echo ""
            echo "This job must pull that tag from GitHub Container Registry before **unit-tests** runs."
            echo "The tag is created by the **build-base-container** workflow when \`Dockerfile\` changes on the default branch."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull base image from GHCR
        run: |
          IMAGE="${{ steps.resolve.outputs.image }}"
          echo "Pulling: ${IMAGE}"
          if docker pull "${IMAGE}"; then
            echo "Base image is available."
            exit 0
          fi
          echo ""
          echo "::error title=Base image not found in GHCR::Could not pull \`${IMAGE}\`. Nothing was found at that tag (or the registry denied access)."
          exit 1

  # Tests unitarios corren DENTRO del contenedor base (sin Docker).
  unit-tests:
    name: Unit tests (in container)
    needs: ensure-base-image
    runs-on: ubuntu-latest
    container:
      image: ${{ needs.ensure-base-image.outputs.image }}
      credentials:
        username: ${{ github.repository_owner }}
        password: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install project in editable mode
        run: pip install --no-cache-dir -e .[test]

      - name: Run Unit Tests
        run: make test-unit

      - name: Run AI Agent Tests
        run: make test-agents

  # Tests de integración corren en el runner nativo donde Docker está disponible.
  integration-tests:
    name: Integration tests (docker compose)
    needs: ensure-base-image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install project in editable mode
        run: pip install --no-cache-dir -e .[test]

      - name: Run Integration Tests
        run: make test-integration
